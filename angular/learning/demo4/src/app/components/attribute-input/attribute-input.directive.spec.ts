import { AttributeInputDirective } from './attribute-input.directive';

describe('AttributeInputDirective', () => {
  it('should create an instance', () => {
    const directive = new AttributeInputDirective();
    expect(directive).toBeTruthy();
  });
});
